# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

个人用户在本机整理 Markdown 笔记、技术文档和图文资料。

## Product Purpose

Standalone Editor 直接读取用户选择的本地目录，让用户在浏览器中浏览、编辑、整理和保存 Markdown 文件。成功意味着用户可以快速找到文件，专注编辑，并且始终知道内容是否已经写回磁盘。

## Positioning

文件保留在用户自己的目录中，编辑器通过本地服务提供轻量的文档工作台，不要求迁移到云端数据库或改变既有文件结构。

## Operating Context

桌面浏览器是主要场景，移动浏览器提供基本查看和编辑能力。目录路径按操作系统选择器返回的真实路径展示，当前工作目录保存在浏览器中以便下次恢复。

## Capabilities and Constraints

- 支持文件树、搜索、多标签、Markdown 富文本和源文本编辑。
- 支持图片、表格、任务列表、代码块、目录拖拽整理和导入导出。
- 编辑内容默认延迟三秒自动保存，也支持手动保存。
- 工作区切换必须先等待当前路径的待保存内容完成写入。
- 目录、文件和图片访问由本地后端校验，界面不能绕过路径边界。

## Evidence on Hand

现有实现位于 `frontend/src` 与 `backend/src`；真实笔记目录不属于项目测试数据，视觉验收使用隔离的临时工作区。

## Product Principles

- 文件属于用户，界面服务于文件结构。
- 编辑时保持安静，状态变化必须清楚。
- 高频操作可见，低频操作保持可发现。
- 桌面效率与移动端可用性一起维护。

## Accessibility & Inclusion

操作按钮需要具备可读的中文标签、悬停提示和键盘焦点；颜色只作为状态的辅助表达，移动端触控目标保持足够尺寸。
