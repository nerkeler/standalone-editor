# Design

## Direction

“安静的本地文档工作台”：B 方案的文件工作台结构，吸收 A 方案的阅读宽度和低干扰编辑体验。界面采用中性石墨灰深色主题与暖纸色浅色主题，正文拥有独立的阅读节奏，工具和状态有明确分区。

## Mode

Operate + Read。文件定位、编辑和整理是主要任务，阅读长文时正文优先。

## Layout

- 桌面左侧文件栏默认约 280px，可拖动调整，底部仅显示当前目录路径。
- 中心区域包含标签、紧凑分组工具栏和 720–800px 的正文列。
- 宽屏可切换右侧大纲；窄屏大纲回到目录抽屉内，不抢占正文。
- 底部状态栏持续表达目录和保存状态；主题切换位于左下角且不遮挡路径。

## Visual System

- 低饱和的 graphite / warm paper surfaces，边界用细线和层级对比表达。
- 正文使用系统无衬线，代码和路径使用等宽字体；正文行高舒展，标题有明显层级。
- 图标来自 Ant Design Icons，按钮统一 32–34px 触控尺寸，并且提供中文 tooltip 与 aria-label。
- 动效只用于面板、状态和 hover 过渡，避免分散写作注意力。

## Interaction Rules

- 标题使用一个可读的下拉选择器替代 H1–H6 按钮墙。
- 源文本/编辑模式是明确的切换操作，并保留可测试的 aria-label。
- 保存状态独立可见：已保存、修改待保存、保存中、保存失败；失败提供重试。
- 自动保存和工作区切换的现有安全语义保持不变。
